import { Button } from 'react-bootstrap';

function Settings() {
  return (
      <div>
          <Button variant="primary" className="w-100 mb-2">Settings</Button>
      </div>
  )
}

export default Settings
